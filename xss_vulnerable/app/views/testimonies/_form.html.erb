<%= form_for(@testimony) do |f| %>
  <% if @testimony.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testimony.errors.count, "error") %> prohibited this testimony from being saved:</h2>

      <ul>
      <% @testimony.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :testimony_text %><br>
    <%= f.text_area :testimony_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
